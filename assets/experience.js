(()=>{const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)],api='https://killerzon3-stats.kelvind409.workers.dev';let items=[];const dialog=q('#smart-search'),input=q('#smart-search-input'),results=q('#smart-search-results');const drawSearch=term=>{const value=(term||'').toLowerCase();results.innerHTML=items.filter(p=>!value||`${p.name} ${p.summary} ${p.section}`.toLowerCase().includes(value)).slice(0,15).map(p=>`<a href="package.html?id=${encodeURIComponent(p.id)}"><span>${p.name}</span><small>${p.section||'Tweaks'} →</small></a>`).join('')||'<div class="trend">No tweaks found</div>'};fetch(`packages.json?v=${Date.now()}`).then(r=>r.json()).then(data=>{items=data;drawSearch('');setupCompatibility();loadTrending()});q('#open-smart-search')?.addEventListener('click',()=>{dialog.showModal();input.focus()});input?.addEventListener('input',e=>drawSearch(e.target.value));dialog?.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});addEventListener('keydown',e=>{if(e.key==='/'&&!/INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName)){e.preventDefault();dialog.showModal();input.focus()}if(e.key==='Escape'&&dialog?.open)dialog.close()});const observer=new IntersectionObserver(entries=>entries.forEach(e=>e.target.classList.toggle('is-visible',e.isIntersecting)),{threshold:.08});qa('.section,.experience-card,.roadmap-item').forEach(el=>{el.classList.add('reveal');observer.observe(el)});if(matchMedia('(pointer:fine)').matches){const glow=document.createElement('i');glow.className='pointer-glow';document.body.append(glow);addEventListener('pointermove',e=>{glow.style.left=`${e.clientX}px`;glow.style.top=`${e.clientY}px`});document.addEventListener('pointermove',e=>{const card=e.target.closest('.package-card');if(!card)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*4}deg) rotateY(${x*5}deg) translateY(-4px)`});document.addEventListener('pointerout',e=>{const card=e.target.closest('.package-card');if(card)card.style.transform=''})}function setupCompatibility(){const ios=q('#compat-ios'),jb=q('#compat-jailbreak'),out=q('#compat-result');if(!ios)return;ios.innerHTML='<option value="">Choose iOS version</option>'+Array.from({length:14},(_,i)=>`<option value="${i+13}">iOS ${i+13}</option>`).join('');const check=()=>{if(!ios.value){out.textContent='Choose your iOS version and jailbreak.';return}const version=Number(ios.value),matches=items.filter(p=>{const values=(p.compatibility||'').match(/\d+(?:\.\d+)?/g)?.map(Number)||[];return !values.length||version>=values[0]&&(values.length<2||version<=values[1])});out.innerHTML=matches.length?`<b>${matches.length} possible matches.</b> Check each tweak page before installing on ${jb.value}.`:'No confirmed matches yet. Send Kelvin a compatibility request.'};ios.onchange=check;jb.onchange=check}function loadTrending(){fetch(`${api}/stats`).then(r=>r.json()).then(data=>{const rows=Object.entries(data.packages||{}).sort((a,b)=>b[1]-a[1]).slice(0,4),box=q('#trending-list');box.innerHTML=rows.map(([id,views],i)=>{const p=items.find(x=>x.id===id);return `<a class="trend" href="package.html?id=${encodeURIComponent(id)}"><span>${i+1}. ${p?.name||id}</span><b>${views} views</b></a>`}).join('')||'<div class="trend">Stats are warming up.</div>'}).catch(()=>{})}if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});})();
